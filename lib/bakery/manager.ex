defmodule Manager do
  def open_store(num_customers, num_servers) do
    customers = #TODO
    servers = #TODO
    start_serving(customers, servers)
  end

  def start_serving(customers, servers) do
    #TODO
  end
end
